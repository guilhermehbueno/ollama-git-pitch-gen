#!/usr/bin/env bash
set -euo pipefail

case "${1:-}" in
  cols)
    echo "${TPUT_COLS:-80}"
    ;;
  lines)
    echo "${TPUT_LINES:-24}"
    ;;
  *)
    echo "Unsupported tput capability: $1" >&2
    exit 1
    ;;
esac
